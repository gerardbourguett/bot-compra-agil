# Kubernetes Secrets - NO APLICAR ESTE ARCHIVO DIRECTAMENTE
# Usar el comando kubectl create secret para crear desde GitHub Secrets

# Comando para crear el secret:
# kubectl create secret generic compraagil-secretsnamespace apps \
#   --from-literal=DATABASE_URL="postgresql://compra_agil_user:PASSWORD@64.176.19.51:5433/compra_agil" \
#   --from-literal=GEMINI_API_KEY="tu_api_key" \
#   --from-literal=TELEGRAM_TOKEN="tu_token"

apiVersion: v1
kind: Secret
metadata:
  name: compraagil-secrets
  namespace: apps
type: Opaque
stringData:
  # ESTOS SON VALORES DE EJEMPLO - NO USAR EN PRODUCCIÓN
  # Crear el secret real con el comando kubectl arriba
  DATABASE_URL: "postgresql://user:pass@host:port/db"
  GEMINI_API_KEY: "your-key-here"
  TELEGRAM_TOKEN: "your-token-here"

